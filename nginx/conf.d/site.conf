##### DEPRECATED FILE ####
# This file intentionally left inert. See `smartergpt.dev.conf` for the active
# server blocks. Retained only to avoid accidental re-creation and to document
# prior regression source (duplicate conflicting server_name localhost vs domain).
