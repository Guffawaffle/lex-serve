# nginx/conf.d/includes/health.conf
# Included inside the HTTP server in site.conf. Do NOT wrap with 'server{}'.
location = /healthz {
    access_log off;
    default_type text/plain;
    add_header Content-Type text/plain;
    return 200 "ok\n";
}
