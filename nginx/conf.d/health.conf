# health.conf is intentionally empty to avoid being auto-included by the global
# "include /etc/nginx/conf.d/*.conf;" directive. The real location snippet is
# located at `conf.d/includes/health.conf` and is included explicitly from
# `site.conf` inside the HTTP server block to ensure correct context.
