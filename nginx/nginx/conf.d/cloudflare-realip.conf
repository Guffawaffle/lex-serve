# TODO: Populate Cloudflare IPv4 and IPv6 ranges here before enabling real_ip processing.
# Fetch the authoritative list from:
#   https://www.cloudflare.com/ips-v4
#   https://www.cloudflare.com/ips-v6
#
# Example entry (do NOT use as-is; fetch the live list):
# set_real_ip_from 173.245.48.0/20;
# set_real_ip_from 103.21.244.0/22;
# ...
#
# After populating this file, enable the include + real_ip_header in nginx/conf.d/site.conf:
#   include /etc/nginx/conf.d/cloudflare-realip.conf;
#   real_ip_header CF-Connecting-IP;
#
# Note: Do NOT add secrets here. This file is intentionally editable by operators at deploy time.
